import { StaysPage } from '@/components/stays/StaysPage';

export const metadata = {
    title: 'Stays | Kerala Tourism Explorer',
    description: 'Find the perfect resort, hotel, homestay, or houseboat for your Kerala trip.',
};

export default function Page() {
    return <StaysPage />;
}
